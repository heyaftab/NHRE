<?php
require_once __DIR__ . '/auth/auth_check.php';require_auth();ensure_clinical_tables();$id=(int)($_GET['id']??0);$s=db()->prepare('SELECT related_url FROM notifications WHERE id=? AND user_id=?');$s->execute([$id,(int)$_SESSION['user_id']]);$url=(string)$s->fetchColumn();if($url===''){$_SESSION['errors']=['Notification not found.'];redirect('notifications.php');}db()->prepare('UPDATE notifications SET is_read=1 WHERE id=? AND user_id=?')->execute([$id,(int)$_SESSION['user_id']]);if(str_contains($url,'://')||str_starts_with($url,'/')||str_starts_with($url,'..'))$url='notifications.php';redirect($url);
